type Temperature @model @key(name: "ByCreatedAt", fields: [ "type", "createdAt" ], queryField: "getTemperatureByCreatedAt") {
  id: ID!
  type: String!
  sub: String!
  name: String!
  temperature: Float!
  url: String
  createdAt: AWSDateTime!
}
